# Copy to .env and fill in from the headset.
# Quest: Settings → System → Developer → Wireless debugging

# Wireless connect target (not the pairing port)
QUEST_HOST=
QUEST_PORT=5555

# Package name used by logs / launch once an APK is installed
PACKAGE=com.saltmarshdigital.redrock
